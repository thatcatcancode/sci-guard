# Node modules (reinstalled in Docker anyway)
node_modules
npm-debug.log
yarn-error.log

# Vite / CRA local build output (we’ll re-create this in the container)
dist
build

# OS or editor files
.DS_Store
.env.*
!.env.production  # Allow production env through

# Git files
.git
.gitignore

# Docker config (if irrelevant inside build)
Dockerfile
.dockerignore
